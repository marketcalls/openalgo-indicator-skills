# OpenAlgo API Configuration
OPENALGO_API_KEY=your_openalgo_api_key_here
OPENALGO_HOST=http://127.0.0.1:5000

# WebSocket Configuration (optional - defaults to host-based derivation)
# OPENALGO_WS_URL=ws://127.0.0.1:8765

# yfinance (no API key needed - free public data)
# Used for US/Global market data when OpenAlgo is not available
